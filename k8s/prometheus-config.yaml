apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: redfish-exporter
        static_configs:
          - targets: ["redfish-exporter:9812"]
